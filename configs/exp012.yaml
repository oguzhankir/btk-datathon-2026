exp_id: exp012
description: "Stronger text model: XLM-RoBERTa-large fine-tune, regression head (GPU)"
model: bert
bert: { model: FacebookAI/xlm-roberta-large, epochs: 6, lr: 1.0e-5, max_len: 192, batch_size: 16, inner_val_frac: 0.1 }
allow_cpu: false
target_transform: none
sample_weighting: none
n_seeds: 1
model_params: {}
hpo: { trials: 0, timeout: null }
