exp_id: exp005
description: "CatBoost on exp004 feature set (native cats, GPU-capable)"
model: catboost
model_params_file: null
features:
  tabular_fe: true
  target_encoding: true
  text_classic: true
  ridge_meta: true
  embeddings: { model: intfloat/multilingual-e5-large, svd: 64, knn: [5, 25] }
target_transform: none
sample_weighting: none
n_seeds: 1
model_params: {}                   # inline param overrides (merged over model_params_file)
hpo: { trials: 0, timeout: null }  # trials > 0 -> Optuna-tune first (CLI --hpo-trials overrides)
