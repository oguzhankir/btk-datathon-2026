exp_id: exp009
description: "BERT fine-tune Turkish (best-epoch selection, longer): regression head (GPU)"
model: bert
bert: { model: dbmdz/bert-base-turkish-cased, epochs: 8, lr: 2.0e-5, max_len: 192, batch_size: 32, inner_val_frac: 0.1 }
allow_cpu: false
target_transform: none
sample_weighting: none
n_seeds: 1
model_params: {}
hpo: { trials: 0, timeout: null }
