exp_id: exp018
description: "Text diversity: xlm-roberta-base fine-tune (lighter than large, different capacity)"
model: bert
bert: { model: FacebookAI/xlm-roberta-base, epochs: 5, lr: 2.0e-5, max_len: 128, batch_size: 32, inner_val_frac: 0.1, max_grad_norm: 1.0 }
allow_cpu: false
target_transform: none
sample_weighting: none
n_seeds: 1
model_params: {}
hpo: { trials: 0, timeout: null }
