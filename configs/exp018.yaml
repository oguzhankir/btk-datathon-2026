exp_id: exp018
description: "Text diversity: mDeBERTa-v3-base fine-tune (disentangled attention, different errors than BERT/XLM-R)"
model: bert
bert: { model: microsoft/mdeberta-v3-base, epochs: 6, lr: 5.0e-6, max_len: 192, batch_size: 16, inner_val_frac: 0.1, max_grad_norm: 1.0 }
allow_cpu: false
target_transform: none
sample_weighting: none
n_seeds: 1
model_params: {}
hpo: { trials: 0, timeout: null }
