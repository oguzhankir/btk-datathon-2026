exp_id: exp021
description: "Text v2 recipe on xlm-roberta-base (same recipe as exp020, different backbone for blend diversity)"
model: bert_v2
bert: { model: FacebookAI/xlm-roberta-base, epochs: 8, lr: 2.0e-5, head_lr: 1.0e-3, llrd: 0.9,
        max_len: 128, batch_size: 32, n_dropout: 5, p_dropout: 0.3, inner_val_frac: 0.1, max_grad_norm: 1.0 }
allow_cpu: false
target_transform: none
sample_weighting: none
n_seeds: 1
model_params: {}
hpo: { trials: 0, timeout: null }
