# Notebooks

Placeholder: `final_solution.ipynb` will be assembled here at the end of the
competition from the pipeline in `src/` + the winning config/blend.
Pipeline code lives in `src/` and `scripts/` — notebooks are for EDA and the
final writeup only (see `reports/eda/` for committed EDA scripts).
